import fs from 'node:fs';import vm from 'node:vm';import assert from 'node:assert/strict';
const source=fs.readFileSync('lesson-experience-v72.js','utf8'),css=fs.readFileSync('lesson-experience-v72.css','utf8'),index=fs.readFileSync('index.html','utf8'),sw=fs.readFileSync('sw.js','utf8');
const document={addEventListener(){},querySelector(){return null},querySelectorAll(){return[]},createElement(){return{innerHTML:'',querySelector(){return null},querySelectorAll(){return[]},addEventListener(){},remove(){},setAttribute(){}}},body:{appendChild(){}}};
const sandbox={console,document,localStorage:{getItem(){return null},setItem(){}},setTimeout(){return 0},speechSynthesis:{cancel(){},speak(){},getVoices(){return[]}},SpeechSynthesisUtterance:class{},navigator:{}};sandbox.window=sandbox;vm.createContext(sandbox);vm.runInContext(source,sandbox,{filename:'lesson-experience-v72.js'});
const a=sandbox.auditLessonExperienceV72();assert.equal(a.version,'v72-experience');assert.equal(a.totalLessons,210);assert.equal(a.teachingStages,8);assert.equal(a.curriculumFirst,true);assert.equal(a.teacherLedThai,true);assert.equal(a.lessonObjectivesFirst,true);assert.equal(a.contextualVocabulary,true);assert.equal(a.grammarUsageExplanation,true);assert.equal(a.commonMistakeGuidance,true);assert.equal(a.naturalDialogue,true);assert.equal(a.femaleMaleVoices,true);assert.equal(a.naturalVoiceScoring,true);assert.equal(a.miniMission,true);assert.equal(a.guidedProduction,true);assert.equal(a.strictTeachTestSeparation,true);assert.equal(a.quizQuestions,8);assert.equal(a.quizPass,6);assert.equal(a.finalVoiceNewSentence,true);assert.equal(a.databaseAudioStorage,false);assert.equal(a.ok,true);
for(const text of ['วันนี้เรียนอะไร','ครูอธิบายวิธีใช้','คำศัพท์ที่ใช้ในบทนี้','จากโครงสร้างสู่ประโยคจริง','ฟังบทสนทนาและตอบรับกัน','Mini Mission','ฝึกสร้างประโยคของตัวเอง','สรุปแบบอาจารย์ก่อนเข้าสอบ','ส่วนเรียนจบแล้ว','Final Voice'])assert(source.includes(text),`missing ${text}`);
assert(source.includes('natural|neural|premium|enhanced|online'));assert(source.includes("gender==='male'"));assert(source.includes('SpeechRecognition'));assert(!source.includes('MutationObserver'));
assert(css.includes('.v72-teacher')&&css.includes('.v72-dialog')&&css.includes('.v72-mission')&&css.includes('@media(max-width:620px)'));
assert(index.includes('lesson-experience-v72.css?v=72e'));assert(index.includes('lesson-experience-v72.js?v=72e'));assert(index.indexOf('curriculum-v72.js?v=72')<index.indexOf('lesson-experience-v72.js?v=72e'));
assert(sw.includes('./lesson-experience-v72.css?v=72e')&&sw.includes('./lesson-experience-v72.js?v=72e'));
console.log(JSON.stringify({ok:true,version:'v72-experience',teacherLed:true,stages:8,naturalVoice:true,quiz:'6/8',finalVoice:true},null,2));
