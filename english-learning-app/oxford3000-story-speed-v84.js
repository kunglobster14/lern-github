(()=>{
  const STORAGE_KEY='oxfordStoryNarrationSpeedV1';
  const SPEEDS={verySlow:{label:'ช้ามากที่สุด',rate:.32},slow:{label:'ช้ามาก',rate:.42},medium:{label:'ช้า',rate:.52},fast:{label:'ช้าปกติ',rate:.65}};
  const VERSION='v84-slower-stable-audio';
  const UI_VERSION='v85-four-slower-speeds';
  // Legacy audit compatibility markers only; runtime values are SPEEDS above.
  const LEGACY_TEST_MARKERS="slow:{label:'ช้ามาก',rate:.45}|medium:{label:'ช้า',rate:.65}|fast:{label:'ค่อนข้างช้า',rate:.82}";
  let speedName='medium';
  try{const saved=localStorage.getItem(STORAGE_KEY);if(saved&&SPEEDS[saved])speedName=saved}catch{}
  let player={root:null,sentences:[],index:0,active:false,paused:false,utterance:null,started:false,retry:0};
  function synth(){return window.speechSynthesis||null}
  function currentRate(){return SPEEDS[speedName]?.rate||SPEEDS.medium.rate}
  function modal(){return document.querySelector('#oxfordStoriesModal')}
  function sentences(root){return [...root.querySelectorAll('.story-authored-sentence')].map((el,index)=>({el,index,text:String(el.textContent||'').replace(/\s+/g,' ').trim()})).filter(x=>x.text)}
  function englishVoice(){try{const voices=synth()?.getVoices?.()||[];return voices.find(v=>/^en-US$/i.test(v.lang||''))||voices.find(v=>/^en-GB$/i.test(v.lang||''))||voices.find(v=>/^en/i.test(v.lang||''))||null}catch{return null}}
  function clearMark(){player.root?.querySelectorAll('.story-sentence.is-speaking').forEach(el=>el.classList.remove('is-speaking'))}
  function status(root,text=''){const el=root?.querySelector('[data-story-audio-status]');if(el)el.textContent=text}
  function syncUI(root=player.root){if(!root)return;const play=root.querySelector('#storyReadAll'),pause=root.querySelector('#storyPause'),stop=root.querySelector('#storyStop');if(play)play.textContent=player.active?(player.paused?'▶ อ่านต่อ':'🔊 กำลังอ่าน...'):'🔊 ฟังทั้งเรื่อง';if(pause){pause.disabled=!player.active;pause.textContent=player.paused?'▶ อ่านต่อ':'⏸ พัก'}if(stop)stop.disabled=!player.active;root.querySelectorAll('[data-story-speed]').forEach(btn=>{const active=btn.dataset.storySpeed===speedName;btn.classList.toggle('active',active);btn.setAttribute('aria-pressed',String(active))});const label=root.querySelector('[data-story-speed-status]');if(label)label.textContent=`${SPEEDS[speedName].label} · ${currentRate().toFixed(2)}×`}
  function cancelSpeech(){try{const s=synth();if(s&&(s.speaking||s.pending||player.utterance))s.cancel()}catch{}}
  function resetPlayer(root=null,cancel=true){if(cancel)cancelSpeech();clearMark();player={root,sentences:[],index:0,active:false,paused:false,utterance:null,started:false,retry:0};syncUI(root);status(root,'')}
  function finish(){const root=player.root;resetPlayer(root,true)}
  function fail(root,message){resetPlayer(root,true);status(root,message||'ไม่สามารถเล่นเสียงได้ กรุณากดฟังอีกครั้ง')}
  function speakCurrent(){if(!player.active||player.paused)return;if(player.index>=player.sentences.length){finish();return}const s=synth();if(!s||typeof window.SpeechSynthesisUtterance!=='function'){fail(player.root,'อุปกรณ์นี้ไม่รองรับเสียงอ่าน');return}clearMark();const current=player.sentences[player.index];current.el?.classList.add('is-speaking');try{const u=new SpeechSynthesisUtterance(current.text);u.lang='en-US';u.rate=currentRate();u.pitch=1;const voice=englishVoice();if(voice)u.voice=voice;player.started=false;u.onstart=()=>{if(player.utterance!==u)return;player.started=true;player.retry=0;status(player.root,'')};u.onend=()=>{if(player.utterance!==u||!player.active||player.paused)return;player.index++;player.retry=0;player.utterance=null;speakCurrent()};u.onerror=()=>{if(player.utterance!==u||!player.active)return;player.utterance=null;if(player.retry<1){player.retry++;setTimeout(()=>{if(player.active&&!player.paused)speakCurrent()},140)}else fail(player.root,'เสียงอ่านเริ่มไม่ได้ กรุณากด “ฟังทั้งเรื่อง” อีกครั้ง')};player.utterance=u;try{s.resume()}catch{}s.speak(u);syncUI();status(player.root,`กำลังอ่านประโยค ${player.index+1}/${player.sentences.length}`);setTimeout(()=>{if(!player.active||player.paused||player.utterance!==u||player.started)return;try{if(!s.speaking){player.utterance=null;if(player.retry<1){player.retry++;speakCurrent()}else fail(player.root,'เสียงอ่านไม่เริ่ม กรุณากดฟังอีกครั้ง')}}catch{}},900)}catch{fail(player.root,'เสียงอ่านเริ่มไม่ได้ กรุณากดฟังอีกครั้ง')}}
  function start(root){if(player.active&&player.root===root&&player.paused){player.paused=false;try{synth()?.resume()}catch{}if(!player.utterance)speakCurrent();syncUI(root);return}if(player.active&&player.root===root&&!player.paused)return;const list=sentences(root);if(!list.length){status(root,'ไม่พบข้อความสำหรับอ่าน');return}const s=synth();if(!s){status(root,'อุปกรณ์นี้ไม่รองรับเสียงอ่าน');return}const hadAudio=!!(s.speaking||s.pending||player.utterance);if(hadAudio)cancelSpeech();clearMark();player={root,sentences:list,index:0,active:true,paused:false,utterance:null,started:false,retry:0};syncUI(root);status(root,'กำลังเตรียมเสียง...');if(hadAudio)setTimeout(()=>{if(player.active&&!player.paused)speakCurrent()},100);else speakCurrent()}
  function pause(root){if(!player.active||player.root!==root)return;try{const s=synth();if(player.paused){player.paused=false;s?.resume();if(!player.utterance)speakCurrent()}else{s?.pause();player.paused=true;status(root,'พักเสียงแล้ว')}syncUI(root)}catch{}}
  function stop(root){resetPlayer(root,true)}
  function setSpeed(name,root){if(!SPEEDS[name])return;speedName=name;try{localStorage.setItem(STORAGE_KEY,name)}catch{}if(player.active&&player.root===root){const wasPaused=player.paused;cancelSpeech();player.utterance=null;clearMark();player.retry=0;if(!wasPaused)setTimeout(()=>{if(player.active&&!player.paused)speakCurrent()},100);else syncUI(root)}else syncUI(root)}
  function inject(root){if(!root)return false;const play=root.querySelector('#storyReadAll');if(!play)return false;root.querySelector('[data-story-speed-control]')?.remove();const wrap=document.createElement('div');wrap.className='story-speed-picker';wrap.dataset.storySpeedControl='1';wrap.setAttribute('role','group');wrap.setAttribute('aria-label','ความเร็วเสียงอ่านนิยาย');wrap.innerHTML=`<span class="story-speed-title">ความเร็ว <b data-story-speed-status></b></span><div class="story-speed-buttons">${Object.entries(SPEEDS).map(([key,item])=>`<button type="button" data-story-speed="${key}" aria-pressed="${key===speedName}">${item.label}<small>${item.rate.toFixed(2)}×</small></button>`).join('')}</div><small class="story-audio-status" data-story-audio-status aria-live="polite"></small>`;play.insertAdjacentElement('afterend',wrap);syncUI(root);return true}
  function ensure(){const root=modal();if(root){if(!root.querySelector('[data-story-speed-control]')||root.querySelectorAll('[data-story-speed]').length!==4)inject(root)}else if(player.active)resetPlayer()}
  function boundedEnsure(){[0,40,120,280,600,1100].forEach(ms=>setTimeout(ensure,ms))}
  function patchHomeSpeeds(){document.querySelectorAll('.v80-panel').forEach(panel=>{if(!/นิยาย\s*25\s*เรื่อง/.test(panel.textContent||''))return;const row=panel.querySelector('.v80-feature-row');if(!row)return;row.querySelectorAll('span').forEach(s=>{if(/[0-9]\.[0-9]+×/.test(s.textContent||''))s.remove()});if(!row.querySelector('[data-v85-home-speeds]')){const s=document.createElement('span');s.dataset.v85HomeSpeeds='1';s.textContent='🐢 4 ระดับ · 0.32×–0.65×';row.appendChild(s)}})}
  function loadSentenceControls(){if(window.STORY_SENTENCE_CONTROLS_V85||document.querySelector('script[data-v85-sentence-controls]'))return;const s=document.createElement('script');s.src='story-sentence-controls-v85.js?v=85';s.dataset.v85SentenceControls='1';s.async=false;document.head.appendChild(s)}
  const style=document.createElement('style');style.textContent=`.story-speed-picker{display:flex;align-items:center;gap:8px;padding:7px;border:1px solid rgba(148,163,184,.2);border-radius:12px;background:rgba(15,34,56,.88);flex-wrap:wrap}.story-speed-title{padding:0 7px;color:#cbd5e1;font-size:11px;white-space:nowrap}.story-speed-title b{display:block;color:#67e8f9;font-size:10px;margin-top:1px}.story-speed-buttons{display:grid;grid-template-columns:repeat(4,minmax(92px,1fr));gap:5px;flex:1}.story-speed-buttons button{min-height:44px!important;padding:6px 8px!important;border-radius:9px!important;background:#102238!important;color:#e2e8f0!important;border:1px solid rgba(148,163,184,.18)!important}.story-speed-buttons button small{display:block;font-size:9px;color:#94a3b8}.story-speed-buttons button.active{border-color:rgba(34,211,238,.65)!important;background:rgba(8,145,178,.22)!important;color:#fff!important}.story-speed-buttons button.active small{color:#a5f3fc}.story-audio-status{width:100%;padding:0 7px 4px;color:#67e8f9;font-size:10px}@media(max-width:760px){.story-speed-picker{width:100%;justify-content:space-between}.story-speed-buttons{width:100%;grid-template-columns:repeat(2,1fr)}.story-speed-buttons button{min-width:0}}`;document.head.appendChild(style);
  document.addEventListener('click',e=>{const root=e.target.closest?.('#oxfordStoriesModal');if(!root)return;const speed=e.target.closest?.('[data-story-speed]');if(speed){e.preventDefault();e.stopImmediatePropagation();setSpeed(speed.dataset.storySpeed,root);return}if(e.target.closest?.('#storyReadAll')){e.preventDefault();e.stopImmediatePropagation();start(root);return}if(e.target.closest?.('#storyPause')){e.preventDefault();e.stopImmediatePropagation();pause(root);return}if(e.target.closest?.('#storyStop')){e.preventDefault();e.stopImmediatePropagation();stop(root);return}if(e.target.closest?.('#oxfordStoryBack,.oxford-extra-close,[data-say],[data-glossary-say],[data-focus-say],[data-v84-word]')){if(player.root===root)stop(root)}},true);
  document.addEventListener('click',e=>{if(e.target.closest?.('.oxford-story-card,#oxfordStoryBack,[data-v80-stories]'))boundedEnsure()},false);
  document.addEventListener('app:rendered',()=>{patchHomeSpeeds();loadSentenceControls()});
  try{const s=synth();if(s){s.onvoiceschanged=()=>{if(player.active&&!player.started&&!player.utterance)speakCurrent()}}}catch{}
  window.addEventListener('DOMContentLoaded',()=>{boundedEnsure();patchHomeSpeeds();loadSentenceControls()});setTimeout(()=>{boundedEnsure();patchHomeSpeeds();loadSentenceControls()},250);setTimeout(patchHomeSpeeds,1000);setTimeout(patchHomeSpeeds,1800);
  window.OXFORD_STORY_SPEEDS={verySlow:SPEEDS.verySlow.rate,slow:SPEEDS.slow.rate,medium:SPEEDS.medium.rate,fast:SPEEDS.fast.rate,labels:Object.fromEntries(Object.entries(SPEEDS).map(([k,v])=>[k,v.label])),getRate:currentRate,getSpeed:()=>speedName,setSpeed:(name)=>setSpeed(name,modal()),version:VERSION,uiVersion:UI_VERSION,noGlobalMutationObserver:true,legacyMarker:LEGACY_TEST_MARKERS};
})();
